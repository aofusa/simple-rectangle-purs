{
  "name": "simple-rectangle-purs",
  "version": "1.0.0",
  "description": "Simple RectAngle written in PureScript",
  "scripts": {
    "bundle": "npx spago bundle-app -t ./public/index.js -y",
    "watch:script": "npx spago bundle-app -t ./public/index.js -w",
    "watch:server": "npx live-server ./public",
    "watch": "run-p watch:*"
  },
  "author": "aofusa",
  "license": "Apache-2.0",
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
